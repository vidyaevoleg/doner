<div class="lo-r" ng-init="dropzone()">
	<div class="lo-r-nav">
		<div class="lo-r-nav-select">
<!--			<div class="lo-r-nav-back"><a ng-href="#/home">К списку</a></div>-->
		</div>
	</div>

	<div class="lo-r-fb">
		<div class="lo-r-fb-title">Обратная связь</div>
		Мотив: 
		<select ng-model="new_feedback.feedback_type">
			<option ng-repeat="type in types" value="{{type}}">{{ type }}</option>
		</select>
		<div>
			<textarea class="lo-r-fb-text" ng-model="new_feedback.body"></textarea>
		</div>
		Загрузите изображения, если того требует ситуация
		<div class="lo-r-fb-dropzone add-review-dropzone" ng-init="fixDropzone()">
			<form action="/images" id="media-dropzone" class="dropzone" enctype="multipart/form-data"></form>
		</div>
		<button ng-click="submitFeedback()" class="lo-r-fb-submit">Отправить</button>
	</div>